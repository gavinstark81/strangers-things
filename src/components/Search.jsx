function Search() {
  return (
    <>
      <h4>GAAAAH SEARCH BAR GOES HERE</h4>
    </>
  );
}

export default Search;

//TAKEN FROM CREATEPOST
//MAYBE SIMILAR FORMAT BUT IDK
{
  /* <form
        onSubmit={async (e) => {
          e.preventDefault(); //capital function below bad...
          await createNewPost(
            { price, description, title, willDeliver },
            token
          );
          navigate("/Posts");
        }}
      >
        <input
          value={title}
          placeholder="Title"
          onChange={(e) => setTitle(e.target.value)}
        />

        <button type="submit">Create</button>
</form> */
}
